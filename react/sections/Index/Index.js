import classes from './Index.module.css';

export default function Index() {
    return <section className={classes.index} id="index">
        <div className={classes.intro}>
            <h1>Kushal Upreti</h1>
            <p>I build robust web and mobile products</p>
        </div>
    </section>
}